# Publisher Templates

This directory is intentionally kept as a placeholder in the shared workflow repository.

Add venue-specific submission classes, logos, or manuscript templates only in downstream repository instantiations.
Do not commit publisher-specific assets back into the shared `research-paper-workflow` repository.
